<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.linkee.linkeeapi.notice.mapper.NoticeMapper">

    <!-- NoticeResponseDto에 맞춘 매핑 -->
    <resultMap id="NoticeResponseMap" type="com.linkee.linkeeapi.notice.model.dto.response.NoticeResponseDto">
        <result property="noticeTitle" column="notice_title"/>
        <result property="noticeContent" column="notice_content"/>
        <result property="noticeViews" column="notice_views"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findAll" resultMap="NoticeResponseMap">
        SELECT i.notice_title,
               i.notice_content,
               i.notice_views,
               i.created_at
        FROM tb_notice i
        ORDER BY i.created_at DESC
            LIMIT #{offset}, #{limit}
    </select>

    <!-- 전체 개수 -->
    <select id="countAll" resultType="int">
        SELECT COUNT(*)
        FROM tb_notice
    </select>
</mapper>